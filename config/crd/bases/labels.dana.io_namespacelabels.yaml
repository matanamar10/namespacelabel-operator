---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: namespacelabels.labels.dana.io
spec:
  group: labels.dana.io
  names:
    kind: Namespacelabel
    listKind: NamespacelabelList
    plural: namespacelabels
    singular: namespacelabel
  scope: Namespaced
  versions:
    - name: v1
      schema:
        openAPIV3Schema:
          description: Namespacelabel is the Schema for the namespacelabels API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: NamespacelabelSpec defines the desired state of Namespacelabel
              properties:
                labels:
                  additionalProperties:
                    type: string
                  description: |-
                    INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                    Important: Run "make" to regenerate code after modifying this file
                  type: object
              type: object
            status:
              description: NamespacelabelStatus defines the observed state of Namespacelabel
              properties:
                appliedLabels:
                  additionalProperties:
                    type: string
                  type: object
                lastUpdated:
                  description: LastUpdated is the timestamp of the last successful label
                    update.
                  format: date-time
                  type: string
                message:
                  description: Message provides feedback about the current state, such
                    as success or any errors encountered.
                  type: string
                skippedLabels:
                  additionalProperties:
                    type: string
                  type: object
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: { }